export interface StlFileBaseInfo {
  id : number,
  fileName : string,
}

export interface StlFileDetails{
  id: number,
  fileName: string,
  storagePath:string,
  sizeBytes:number,
  createdAt:number
}
